#version 450

// T

layout(local_size_x=1024) in;

buffer Y {
    T y[];
};

layout(push_constant) uniform PushConsts {
    T x;
    uint n;
};

void main() {
    uint gid = gl_GlobalInvocationID.x;
    if (gid < n) {
        y[gid] = T(x);
    }
}
